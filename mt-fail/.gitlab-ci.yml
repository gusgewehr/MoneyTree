image: tiangolo/docker-with-compose

before_script:
  - docker login -u gitlab-ci-token -p $CI_JOB_TOKEN $CI_REGISTRY
  - pip install docker-auto-labels

stages:
  - test
  - build
  - deploy

tests:
  stage: test
  script:
    - sh ./scripts/test.sh
  tags:
    - build
    - test

build-stag:
  stage: build
  script:
    - TAG=stag FRONTEND_ENV=staging sh ./scripts/build-push.sh
  only:
    - master
  tags:
    - build
    - test
    
build-prod:
  stage: build
  script:
    - TAG=prod FRONTEND_ENV=production sh ./scripts/build-push.sh
  only:
    - production
  tags:
    - build
    - test

deploy-stag:
  stage: deploy
  script: 
    - >
      DOMAIN=stag.moneytree.gusgewehr.com
      TRAEFIK_TAG=stag.moneytree.gusgewehr.com
      STACK_NAME=stag-moneytree-gusgewehr-com
      TAG=stag
      sh ./scripts/deploy.sh
  environment:
    name: staging
    url: https://stag.moneytree.gusgewehr.com
  only:
    - master
  tags:
    - swarm
    - stag

deploy-prod:
  stage: deploy
  script:
    - >
      DOMAIN=moneytree.gusgewehr.com
      TRAEFIK_TAG=moneytree.gusgewehr.com
      STACK_NAME=moneytree-gusgewehr-com
      TAG=prod
      sh ./scripts/deploy.sh
  environment:
    name: production
    url: https://moneytree.gusgewehr.com
  only:
    - production
  tags:
    - swarm
    - prod
